Setting up for development
--------------------------

- Install the site

    shell> drush si emergency_site --db-url=mysql://root:root@localhost/humanintarian_response --site-name="Humanitarian Response" --account-pass=admin -y
    shell> drush si emergency_site --db-url=mysql://internews:XvMnw2#a@127.0.0.1/in_dev_wiredcraft_com --site-name="Humanitarian Response" --account-pass=admin -y

- Download message library related modules

    shell> cd sites/all/modules/
    shell> git clone git@github.com:Wiredcraft/message_library.git message_library
    shell> cd message_library

    shell> cd DRUPALROOT
    shell> drush make --no-core sites/all/modules/message_library/message_library_migrate/message_library_migrate.make -y
    shell> drush make --no-core sites/all/modules/message_library/message_library.make -y

- Download or enable other helping modules

    shell> drush en diff -y
    shell> drush dl devel --dev; drush en devel_generate -y

- Enable relevant modules and features

    shell> drush en message_library
    shell> drush fr message_library -y
    shell> drush cc all

- Prepare the taxonomies
    - Use migrate to prepare some taxonomies and messages
    - shell> drush prepopulate-ml-threat-terms
    - shell> drush prepopulate-ml-message-threat


- Prepare some content for testing

    shell> drush generate-content 600 --kill --types=message

- Make sure solr is configured and working propertly

- Make essential configuration at: `admin/config/search/search_api`



Setting up for dev platform
---------------------------

scp ~/humanitarianresponse-7.x-1.7.tar.gz wiredcraft@chat.dev.wiredcraft.com:/var/www/in.dev.wiredcraft.com

drush si emergency_site --db-url=mysql://internews:XvMnw2#a@127.0.0.1/in_dev_wiredcraft_com --db-su=internews --db-su-pw=XvMnw2#a --site-name="Humanitarian Response" --account-pass=admin -y
